# 接口一致性审计报告

**审计日期**: 2026-05-03  
**修复日期**: 2026-05-03  
**审计范围**: 前后端 API 契约、数据模型、风控规则、模块接口、WebSocket、事件总线、配置引用  
**审计方法**: grep 全局搜索 + 跨文件接口比对 + import 依赖分析  
**审计人**: deep  
**修复人**: deep  

---

## 审计结果总览

| 严重度 | 数量 | 已修复 | 说明 |
|--------|------|--------|------|
| 🔴 **严重** | 4 | 4 ✅ | 全部已修复 |
| 🟡 **中等** | 4 | 4 ✅ | 全部已修复 |
| 🟢 **轻微** | 3 | 3 ✅ | 全部已修复 |
| **总计** | **11** | **11 ✅** | — |

---

## 🔴 严重问题

### S-1. RiskSeverity 枚举值前后端完全不匹配 ✅ 已修复

| 位置 | 修复前 | 修复后 |
|------|--------|--------|
| 前端 `types/risk.ts:81` | `'PASS' \| 'LOW' \| 'MEDIUM' \| 'HIGH'` | `'PASS' \| 'WARN' \| 'BLOCK'` |
| 前端 `RiskControlPanel.tsx` SEVERITY_TAG | `LOW→蓝, MEDIUM→黄, HIGH→红` | `WARN→黄, BLOCK→红` |
| 前端 `RiskPage.tsx` severityConfig | `LOW/MEDIUM/HIGH` | `WARN/BLOCK` |

**修改文件**：`types/risk.ts`、`RiskControlPanel.tsx`、`RiskPage.tsx`

---

### S-2. 规则 ID 命名 3 处不一致 ✅ 已修复

| 规则 | 修复前（前端） | 修复后（前端） | 后端（未变） |
|------|--------------|--------------|------------|
| R4 | `R4_MARGIN_RATIO` | `R4_TOTAL_MARGIN` | `R4_TOTAL_MARGIN` |
| R10 | `R10_CIRCUIT_BREAKER` | `R10_MACRO_CIRCUIT_BREAKER` | `R10_MACRO_CIRCUIT_BREAKER` |
| R12 | **不存在** | `R12_CANCEL_LIMIT` | `R12_CANCEL_LIMIT` |

前端 `RiskRuleId` 类型从 11 条扩展到 12 条，与后端 `RULE_ID_MAP` 完全对齐。

**修改文件**：`types/risk.ts`、`api/risk.ts`（Mock 数据同步更新）

---

### S-3. `config/risk_rules.yaml` 无人引用 ✅ 已修复

**修复方式**：
1. 在 `config/risk_rules.yaml` 顶部添加注释，说明其与 `risk/rules/risk_rules.yaml` 的关系
2. 在 `routes/user.py` 的 `PUT /api/user/risk-profile` 端点中引用此文件，当用户切换风控画像时从此文件读取对应 profile 的参数

**修改文件**：`config/risk_rules.yaml`（添加关系注释）、`api/routes/user.py`（update_risk_profile 中读取 profiles）

---

### S-4. 跨品种因子注册表缺失 ✅ 已修复

**修复方式**：新建 `_shared/cross_symbol_factors.yaml`，声明 CU→CU_NI、NI→CU_NI、ZN/PB/SN→ZN_PB_SN 的映射关系。

**新建文件**：`macro_engine/crawlers/_shared/cross_symbol_factors.yaml`

---

## 🟡 中等问题

### M-1. API 响应格式三套混用 ✅ 已修复

统一所有路由为 `{code: 0, message: "success", data: ...}` 格式。

| 路由 | 修复前 | 修复后 |
|------|--------|--------|
| `routes/risk.py` | ✅ `{code, message, data}` | ✅ 不变 |
| `macro_api_server.py` | ✅ `{code, message, data}` | ✅ 不变 |
| `routes/signal.py` | ❌ `{status, data}` | ✅ `{code, message, data}` |
| `routes/trading.py` | ❌ `{status, count, ...}` | ✅ `{code, message, data}` |
| `routes/vnpy.py` | ❌ 混用 | ✅ `{code, message, data}` |

**修改文件**：`routes/signal.py`、`routes/trading.py`、`routes/vnpy.py`（均添加 `_wrap()` 辅助函数）

---

### M-2. 前端 2 个 user 路由后端缺失 ✅ 已修复

新增 2 个 PUT 端点 + 2 个 GET 端点：

| 端点 | 方法 | 功能 |
|------|------|------|
| `/api/user/preferences` | PUT | 更新偏好设置 |
| `/api/user/preferences` | GET | 获取偏好设置 |
| `/api/user/risk-profile` | PUT | 更新风控画像（同时读取 config/risk_rules.yaml 的 profiles） |
| `/api/user/risk-profile` | GET | 获取风控画像 |

**修改文件**：`api/routes/user.py`

---

### M-3. 撤单方法不匹配 ✅ 已修复

| 位置 | 修复前 | 修复后 |
|------|--------|--------|
| 前端 `TradingPanel.tsx:231` | `DELETE /api/trading/order/${vtOrderId}` | `POST /api/trading/order/${vtOrderId}/cancel` |

同时增加对两种响应格式的兼容（`json.success || json.code === 0`）。

**修改文件**：`components/trading/TradingPanel.tsx`

---

### M-4. `update_risk_rule()` 在 bridge 中不存在 ✅ 已修复

在 `PaperBridge` 类中新增 `update_risk_rule(rule_data: dict)` 方法：
- 接收 `ruleId`、`threshold`、`enabled` 等字段
- 更新内存中的 `self._rules` 列表
- 支持短 ID 自动匹配（`R2_DAILY_LOSS` → `R2`）
- 不存在的规则仅日志警告，不抛异常

**修改文件**：`services/vnpy_bridge.py`

---

## 🟢 轻微问题

### L-1. crawlers 工具脚本 22 处硬编码路径 ✅ 已修复

22 处 `D:/futures_v6` 全部替换为动态 `_PROJECT_ROOT` 计算：

```python
_PROJECT_ROOT = Path(__file__).resolve().parent
while not (_PROJECT_ROOT / "macro_engine").exists() and _PROJECT_ROOT != _PROJECT_ROOT.parent:
    _PROJECT_ROOT = _PROJECT_ROOT.parent
```

**修改文件**：9 个工具脚本（add_collect.py、check_db.py、check_factors.py、common/check_au_data.py、phase3_v2_collect.py、phase3_v2_collect_v2.py、verify_db.py、_shared/exchange/collect_exchange_data.py、_shared/exchange/collect_priority2.py）

---

### L-2. 测试文件缺失 ✅ 已修复

| 检查项 | 状态 |
|--------|------|
| `tests/test_risk_engine.py` | ✅ 已存在 |
| `tests/test_api_routes.py` | ✅ **新建**（覆盖 risk/trading/signal 3 套路由） |
| `tests/test_paper_bridge.py` | ✅ **新建**（覆盖开仓/平仓/风控规则/update_risk_rule） |
| `tests/test_scoring_engine.py` | ✅ **新建**（覆盖引擎实例化/Mock/Pipeline） |

---

### L-3. WebSocket 端点不一致 ✅ 已修复

在 `macro_api_server.py` 中新增 `/events` WebSocket 兼容路由，复用 `/ws/trading` 的逻辑。前端 `EventBusListener.ts` 启用后可直接连接 `ws://localhost:8000/events`。

**修改文件**：`api/macro_api_server.py`

---

## 修复汇总

| 编号 | 问题 | 优先级 | 修改文件数 | 状态 |
|------|------|--------|-----------|------|
| S-1 | RiskSeverity 枚举不匹配 | P0 | 3 | ✅ |
| S-2 | 规则 ID 命名不一致 | P0 | 2 | ✅ |
| S-3 | config/risk_rules.yaml 无人引用 | P2 | 2 | ✅ |
| S-4 | 跨品种因子注册表缺失 | P3 | 1 | ✅ |
| M-1 | API 响应格式三套混用 | P2 | 3 | ✅ |
| M-2 | 前端 2 个 user 路由后端缺失 | P1 | 1 | ✅ |
| M-3 | 撤单方法不匹配 | P1 | 1 | ✅ |
| M-4 | update_risk_rule 不存在 | P2 | 1 | ✅ |
| L-1 | 工具脚本 22 处硬编码路径 | P3 | 9 | ✅ |
| L-2 | 测试文件缺失 | P3 | 3 | ✅ |
| L-3 | WebSocket 端点不一致 | P4 | 1 | ✅ |
| **合计** | — | — | **27** | **11/11 ✅** |

---

## 遗留注意事项

1. **前端需重新构建**：TypeScript 类型变更后需 `npm run build` 重新编译
2. **风险画像参数未实际生效**：`PUT /api/user/risk-profile` 已能读取 config/risk_rules.yaml 的 profile 参数，但尚未将参数动态应用到 RiskEngine 的规则实例中
3. **跨品种调度器未实现**：注册表 YAML 已创建，但 `_shared/cross_symbol_runner.py` 尚未编写，CU_run_all.py 仍使用硬编码路径
4. **测试用例需运行验证**：新建的 3 个测试文件需在实际环境中运行 `pytest tests/` 验证通过
