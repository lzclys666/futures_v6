# 因子采集健康度 — 最终审核验收方案

**制定日期**: 2026-05-03  
**范围**: 31 个品种目录，296 个因子脚本，1 个数据库 (pit_data.db)  
**触发条件**: B3-6.2 (web_utils 复用) + B3-5.x (脚本质量) 改造完成后的全量回归  
**总耗时**: 约 4~6 小时（单人），2 天（含问题修复）

---

## 验收架构

```
                          ┌───────────────────────┐
                          │  L0: 文件系统预检      │  → 5min
                          │  文件完整性 + 基础结构  │
                          └──────────┬────────────┘
                                     ▼
                          ┌───────────────────────┐
                          │  L1: 语法 & 导入验证   │  → 15min
                          │  py_compile + import   │
                          └──────────┬────────────┘
                                     ▼
              ┌───────────────────────────────────────┐
              │  L2: 运行时快速验证（L1 的子集）      │  → 1h
              │  每个品种挑最重要因子，实际采集一次   │
              └──────────────────┬────────────────────┘
                                 ▼
        ┌───────────────────────────────────────────────┐
        │ L3: 数据库审计                               │  → 30min
        │ 检查各因子/品种的最近数据日期、缺失率、异常值 │
        └──────────────────────┬────────────────────────┘
                               ▼
        ┌───────────────────────────────────────────────┐
        │ L4: run_all 集成测试                          │  → 2h
        │ 核心品种全链路跑通                             │
        └──────────────────────┬────────────────────────┘
                               ▼
        ┌───────────────────────────────────────────────┐
        │ L5: L4 降级逻辑专项验证                       │  → 1h
        │ 模拟L1失败→L2/L3/L4链是否正常                 │
        └───────────────────────────────────────────────┘
```

---

## L0：文件系统预检（5min）

**目标**: 确认 web_utils 改造没有破坏文件结构

| 检查项 | 命令 | 验收标准 |
|--------|------|---------|
| L0.1 | `grep -rl "requests.get" macro_engine/crawlers/ --include="*.py" \| grep -v __pycache__ \| grep -v common/` | **0 命中**（全部改为 fetch_url/fetch_json） |
| L0.2 | `grep -rn "import requests" macro_engine/crawlers/ --include="*.py" \| grep -v __pycache__ \| grep -v common/` | **0 命中** |
| L0.3 | `grep -rl "fetch_url\|fetch_json" macro_engine/crawlers/ --include="*.py" \| grep -v __pycache__ \| wc -l` | **≥ 24 个文件**（改造覆盖） |
| L0.4 | 检查 `macro_engine/crawlers/` 根目录 | 无 `.py` 文件散落（应在子目录中） |
| L0.5 | `find macro_engine/crawlers/ -name "*.py.old"` | **0 命中** |
| L0.6 | `find macro_engine/crawlers/ -maxdepth 1 -name "__init__.py"` | **34~35 命中**（每个目录都有） |

---

## L1：语法 & 导入验证（15min）

**目标**: 确保 296 个因子脚本全部可被 Python 正确加载，无 ImportError / SyntaxError

### L1.1 — 批量 `py_compile`（脚本化，5min）

```bash
cd D:/futures_v6/macro_engine/crawlers
find . -name "*.py" -not -path "*/__pycache__/*" -not -path "*/_shared/*" \
       -not -path "*/common/*" -not -name "*_run_all*" -not -name "__init__*" \
       -exec python -m py_compile {} \; 2>&1 | tee /tmp/l1_compile_errors.txt
```

**验收标准**: 无 SyntaxError；如果有 ImportError，记录具体文件名和缺失模块

### L1.2 — 批量 `import` 验证（脚本化，10min）

对每个因子脚本，通过 `importlib` 动态导入模块。由于路径依赖 `sys.path.insert`，需先添加搜索路径：

```python
import sys, os, importlib.util

CRAWLERS = "D:/futures_v6/macro_engine/crawlers"
errors = []

for root, dirs, files in os.walk(CRAWLERS):
    # 跳过系统目录
    if "__pycache__" in root or "common" in root or "_shared" in root:
        continue
    for f in files:
        if not f.endswith(".py") or f.startswith("_") or f == "__init__.py":
            continue
        # 模拟脚本的 sys.path.insert
        sys.path.insert(0, os.path.join(CRAWLERS, os.path.basename(root)))
        sys.path.insert(0, CRAWLERS)
        
        mod_name = f.replace(".py", "")
        try:
            importlib.import_module(mod_name)
        except ImportError as e:
            errors.append((f, str(e)))
        
        # 清理 path 避免膨胀
        sys.path.pop(0)
        sys.path.pop(0)

print(f"通过: {total - len(errors)}/{total}, 失败: {len(errors)}")
for f, e in errors[:20]:
    print(f"  ❌ {f}: {e}")
```

**验收标准**: 通过率 **≥ 95%**（允许少量低频因子因网络库缺失而失败）

### L1.3 — web_utils 一致性检查

```bash
cd D:/fuxxx
# 检查所有 fetch_url 调用的返回值是否被正确处理
grep -rn "fetch_url\|fetch_json" macro_engine/crawlers/ --include="*.py" \
    | grep -v __pycache__ | grep -v common/web_utils
```

手动抽查 10 个文件，确认返回值解包为 `(data, err)` 模式，而非直接用做 `response` 对象。

**常见错误模式**：
```python
# ❌ 错误：没有解包返回值
html = fetch_url(url)  # fetch_url 返回 tuple，html 实际上是 (str, None)

# ❌ 错误：仍把返回值当 response 对象
result = fetch_url(url).text  # tuple 没有 .text 属性

# ✅ 正确
html, err = fetch_url(url)
if err:
    return None
# 然后使用 html
```

---

## L2：运行时快速验证（1h）

**目标**: 每个品种选 1 个最重要的因子脚本，实际运行采集，验证数据能正常写入 pit_data.db

### L2.1 — 核心品种（10 个，0.5h）

按前端信号面板依赖度降序选择：

| 优先级 | 品种 | 选择 | 代表因子脚本 | 验证方法 |
|--------|------|------|------------|---------|
| P0 | **AU** | 黄金旗舰 | `AU_DXY美元指数.py` 或 `AU_SPDR黄金ETF持仓量.py` | `python AU/AU_DXY美元指数.py --auto` |
| P0 | **CU** | 铜 | `CU_计算基差.py` | `python CU/CU_计算基差.py` |
| P0 | **RB** | 螺纹钢 | `RB_抓取净持仓.py` | `python RB/RB_抓取净持仓.py` |
| P0 | **AG** | 白银 | `AG_抓取TIPS.py` | `python AG/AG_抓取TIPS.py` |
| P0 | **RU** | 橡胶 | `RU_抓取现货和基差.py` | `python RU/RU_抓取现货和基差.py` |
| P1 | **J** | 焦炭 | `J_我的钢铁网焦炭现货价格.py` | `python J/J_我的钢铁网焦炭现货价格.py` |
| P1 | **AL** | 铝 | `AL_抓取LME铝价_铝道网.py` | `python AL/AL_抓取LME铝价_铝道网.py` |
| P1 | **NI** | 镍 | `NI_抓取期货持仓量.py` | `python NI/NI_抓取期货持仓量.py` |
| P1 | **TA** | PTA | `TA_抓取BRENT价格.py` | `python TA/TA_抓取BRENT价格.py` |
| P1 | **HC** | 热卷 | `HC_热卷期货净持仓.py` | `python HC/HC_热卷期货净持仓.py` |

**验证标准**: 每个脚本运行后，打印信息中显示 `[OK]` 或 `写入成功`。如果打印 `[WARN]` 或无数据，记录为黄色告警。

### L2.2 — 次核心品种（10 个，0.5h）

| 品种 | 选择理由 | 代表因子脚本 |
|------|---------|------------|
| JM | 焦煤 | `JM_抓取焦煤期货持仓量.py` |
| SA | 纯碱 | `SA_抓取现货价.py` |
| P | 棕榈油 | `P_抓取期货收盘价.py` |
| Y | 豆油 | `Y_进口大豆CNF价.py` |
| ZN | 锌 | `ZN_沪锌期货收盘价.py` |
| PB | 铅 | `PB_沪铅期货收盘价.py` |
| SN | 锡 | `SN_沪锡期货收盘价.py` |
| PP | 聚丙烯 | `PP_聚丙烯期货收盘价.py` |
| EG | 乙二醇 | `EG_乙二醇期货收盘价.py` |
| I | 铁矿石 | `I_抓取期货收盘价.py` |

**验证标准**: 同上，记录每个脚本的 exit code 和输出摘要。

### L2.3 — 跨品种因子（2 个，15min）

| 因子 | 脚本 | 验证 |
|------|------|------|
| CU_NI LME升贴水 | `CU_NI/CU_NI_抓取LME升贴水_EVENT.py` | 确认数据写入 CU/NI 两张表 |
| ZN_PB_SN | `ZN_PB_SN/fetch_lme_zn_pb_sn.py` | 确认数据写入 ZN/PB/SN 三张表 |

---

## L3：数据库审计（30min，全程脚本化）

**目标**: 检查 pit_data.db 中各因子是否有合理的最新数据

### L3.1 — 最近数据日期检查

```sql
-- 查看每个品种最近一次数据更新时间
SELECT symbol, factor_code, MAX(obs_date) as last_data, COUNT(*) as records
FROM factor_data
GROUP BY symbol
ORDER BY last_data DESC;
```

**验收标准**:
- 核心品种(P0/P1)：最近数据应在 **3 天内**（含非交易日）
- 次核心品种：最近数据应在 **7 天内**
- 如果某品种 last_data 超过 14 天 → **红色告警**（该品种因子链断裂）

### L3.2 — 数据缺失率检查

```sql
-- 查看每个品种的采集覆盖率
SELECT 
    symbol,
    COUNT(DISTINCT factor_code) as active_factors,
    MIN(obs_date) as earliest,
    MAX(obs_date) as latest
FROM factor_data
GROUP BY symbol
ORDER BY active_factors DESC;
```

**验收标准**: 应有 **≥ 240 个** 活跃因子（根据系统架构文档）

### L3.3 — 异常值检查

```sql
-- 检查异常大/小的值
SELECT symbol, factor_code, value, obs_date
FROM factor_data
WHERE 
    (symbol = 'AU' AND (value < 0 OR value > 5000)) OR
    (symbol = 'CU' AND (value < 0 OR value > 100000))
ORDER BY value DESC
LIMIT 20;
```

**验收标准**: 无极端异常值（单位错误、小数点错位）

---

## L4：run_all 集成测试（2h）

**目标**: 运行完整品种的 run_all 脚本，确认多因子串联采集正常

### L4.1 — P0 品种 run_all（1h）

| 品种 | run_all | 预计耗时 | 说明 |
|------|---------|---------|------|
| AU | `python AU/AU_run_all.py` | ~3min | 黄金：18 个因子 |
| CU | `python CU/CU_run_all.py` | ~3min | 铜：10 个因子（含 CU_NI） |
| RB | `python RB/RB_run_all.py` | ~2min | 螺纹钢：8 个因子 |
| AG | `python AG/AG_run_all.py` | ~3min | 白银：17 个因子 |
| RU | `python RU/RU_run_all.py` | ~2min | 橡胶：7 个因子 |

每个 run_all 运行后：
- 检查 stderr 无 `[WARN]` 或 `[ERROR]`
- 检查 stdout 所有因子均打印 `[OK] ... 写入成功`
- 如果某个子因子打印 `[L1] 失败: ...` 但 `[OK-L4]` 回补成功 → 🟡 告警（L4 正常）
- 如果某个子因子完全跳过（无输出）→ 🔴 告警（因子断裂）

### L4.2 — P1 品种 run_all（1h）

| 品种 | run_all | 说明 |
|------|---------|------|
| J | `python J/J_run_all.py` | 焦炭，含 Mysteel（反爬风险） |
| AL | `python AL/AL_run_all.py` | 铝，含铝道网（反爬风险） |
| TA | `python TA/TA_run_all.py` | PTA |
| NI | `python NI/NI_run_all.py` | 镍 |
| HC | `python HC/HC_run_all.py` | 热卷 |
| JM | `python JM/JM_run_all.py` | 焦煤 |
| SA | `python SA/SA_run_all.py` | 纯碱 |
| ZN | `python ZN/ZN_run_all.py` | 锌 |
| P | `python P/P_run_all.py` | 棕榈油 |
| PB | `python PB/PB_run_all.py` | 铅 |

验收标准（P0 分档通过率）：
- P0 品种（AU/CU/RB/AG/RU）：>= 90% 子因子成功方可通过
- P1 品种（J/AL/NI/TA/HC/JM/SA/ZN/P/PB）：>= 80% 子因子成功方可通过
- P2 品种（其余 20+ 品种）：>= 70% 子因子成功方可通过
- 任意品种 < 70% 整体不通过（系统性故障）
- L4 降级链正常工作（L1 失败 -> L2/L3/L4 兜底）不计入失败

注：因网络抖动、反爬阻断、数据源临时变更导致的个别因子失败，不影响通过率判定。

---

## L5：降级逻辑专项验证（1h）

**目标**: 故意阻断 L1 数据源，验证 L2/L3/L4 降级链是否正常

### L5.0 — ic_heatmap 覆盖验收（新增强制关卡）

**背景**：ic_heatmap 表现已支持跨品种 IC（target_symbol 字段），当前覆盖 ZN/RU/RB/NI/JM 各 5 行共 25 行，目标覆盖 ≥20 个品种。

**验收标准**：
- 运行 `compute_ic_heatmap.py` 后，ic_heatmap 表品种数 ≥ 20
- 若 < 20，标记为 🔴 阻塞，暂停后续验收
- 根因分析：哪些品种缺少 OHLCV 价格表（需要 jm_futures_ohlcv 等价格数据）

### L5.1 — 阻断测试（3 个代表品种）

对 AU、RB、AL 三个品种，分别修改其关键因子脚本，模拟 L1 失败：

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1 | 在因子脚本中加入 `if "L1" in ...: raise Exception("L1模拟阻断")` | 脚本应自动走 L2 |
| 2 | 同时阻断 L1+L2 | 应自动走 L3/L4 |
| 3 | 运行 run_all | 应打印 `[L1] 失败` → `[L2] 失败` → `[OK-L4] 回补` |
| 4 | 恢复脚本 | git checkout |

**验收标准**: 
- 核心因子的降级链完整（L1→L2→L3→L4→DB回补）
- 降级过程打印清晰的失败原因和回补来源

### L5.2 — web_utils 自定义 headers 完整性测试

对 5 个需要自定义 headers 的源（sina/金十/eastmoney/Mysteel/铝道网）分别执行：
```bash
python -c "
from common.web_utils import fetch_url, fetch_json
# 测试 sina（依赖 Referer）
html, err = fetch_url('https://hq.sinajs.cn/list=hf_DXY',
    headers={'Referer': 'https://finance.sina.com.cn'})
print('sina' if html else 'sina_fail:', err)

# 测试 金十（依赖 X-App-Id）
data, err = fetch_json('https://flash-api.jin10.com/get_flash_chart_data?...',
    headers={'X-App-Id': 'jinseof'})
print('jin10' if data else 'jin10_fail:', err)

# 测试 eastmoney（依赖 Referer）
data, err = fetch_json('https://push2.eastmoney.com/api/qt/stock/get?...',
    headers={'Referer': 'https://quote.eastmoney.com/'})
print('eastmoney' if data else 'eastmoney_fail:', err)
"
```

**验收标准**: 5 个源全部返回数据而非 403/401

---

## 验收报告输出格式

以上 6 层验收结束后，整理为一份结构化报告：

```markdown
# 因子采集健康度验收报告 — 2026-05-03

## 概述
- 品种总数: 31/31 (100%)
- 因子脚本: 296
- 数据库记录数: XXXX
- 验收日期: 2026-05-03

## L0 文件系统: ✅/❌
- requests.get 清零: ✅/❌
- import requests 清零: ✅/❌
- 根目录杂项清理: ✅/❌

## L1 语法: ✅/❌
- py_compile 通过率: XX/296
- import 通过率: XX/296

## L2 运行时: ✅/❌
- P0 品种通过: X/10
- P1 品种通过: X/10
- 跨品种因子通过: X/2

## L3 数据库: ✅/❌
- 最近数据(3天内): X/31
- 数据缺失告警: X个品种
- 异常值告警: X个

## L4 run_all: ✅/❌
- P0 品种总计: X/5
- 子因子成功率: XX/XX

## L5 降级逻辑: ✅/❌
- L1→L2→L4 链: X/3 通过
- 自定义 headers: X/5 源正常

## 遗留问题
| # | 严重度 | 品种 | 问题 | 原因 |
|---|--------|------|------|------|
```

---

## 附录：批量验收脚本

将以下脚本保存为 `tools/run_verification.py`，可一键执行 L1/L3 的批量验证：

```python
#!/usr/bin/env python3
"""因子采集健康度验收 — 批量验证脚本"""
import sys, os, importlib, subprocess, sqlite3, json
from pathlib import Path
from datetime import datetime, timedelta

CRAWLERS = Path("D:/futures_v6/macro_engine/crawlers")
DB_PATH = Path("D:/futures_v6/macro_engine/pit_data.db")

results = {"l0": {}, "l1": {}, "l3": {}}

# === L0: 文件系统检查 ===
print("[L0] 检查 requests.get 残留...")
result = subprocess.run(
    "grep -rl 'requests.get' macro_engine/crawlers/ --include='*.py' 2>/dev/null | grep -v __pycache__ | grep -v common/ | wc -l",
    shell=True, capture_output=True, text=True, cwd=CRAWLERS.parent.parent
)
results["l0"]["requests_get_remaining"] = int(result.stdout.strip())
results["l0"]["pass"] = (results["l0"]["requests_get_remaining"] == 0)

# === L1: 批量 import 验证 ===
print("[L1] 批量导入验证...")
sys.path.insert(0, str(CRAWLERS))
errors = []
total = 0

for root, dirs, files in os.walk(CRAWLERS):
    if "__pycache__" in root or "common" in root or "_shared" in root:
        continue
    for f in files:
        if not f.endswith(".py") or f.startswith("_") or f in ("__init__.py",):
            continue
        total += 1
        # 模拟 sys.path.insert 行为
        sub_dir = os.path.basename(root)
        sys.path.insert(0, os.path.join(CRAWLERS, sub_dir))
        sys.path.insert(0, str(CRAWLERS))
        mod_name = f.replace(".py", "")
        try:
            importlib.import_module(mod_name)
        except ImportError as e:
            errors.append({"file": f, "dir": sub_dir, "error": str(e).split("\n")[0][:80]})
        sys.path.pop(0)
        sys.path.pop(0)

results["l1"]["total"] = total
results["l1"]["passed"] = total - len(errors)
results["l1"]["errors"] = errors[:30]
results["l1"]["pass"] = (len(errors) / total <= 0.05)  # ≤5% 通过

# === L3: 数据库审计 ===
print("[L3] 数据库审计...")
conn = sqlite3.connect(str(DB_PATH))
cursor = conn.cursor()

# 最近数据日期
cursor.execute("""
    SELECT symbol, MAX(obs_date) as last_data, COUNT(DISTINCT factor_code) as factors
    FROM factor_data GROUP BY symbol ORDER BY last_data DESC
""")
db_summary = cursor.fetchall()
results["l3"]["total_symbols"] = len(db_summary)
results["l3"]["recent_3d"] = sum(
    1 for s, d, _ in db_summary 
    if d and (datetime.strptime(d, "%Y-%m-%d").date() >= (datetime.now() - timedelta(days=3)).date())
)

# 异常值告警
cursor.execute("""
    SELECT symbol, factor_code, value, obs_date FROM factor_data
    WHERE (symbol = 'AU' AND (value < 0 OR value > 5000))
       OR (symbol = 'CU' AND (value < 0 OR value > 100000))
    ORDER BY value DESC LIMIT 20
""")
results["l3"]["anomalies"] = cursor.fetchall()
conn.close()

# === 输出 JSON ===
print(json.dumps(results, indent=2, default=str))
```

将此脚本复制到项目 `tools/` 目录下执行即可获得 L0/L1/L3 的一键验收报告。

---

## 预期工作量

| 层级 | 耗时 | 自动化程度 | 执行者 |
|------|------|-----------|--------|
| L0 | 5min | 全脚本化 | 任何人 |
| L1 | 15min | 全脚本化 | 任何人 |
| L2 | 1h | 半手动 | 开发人员 |
| L3 | 30min | 全脚本化 | 任何人 |
| L4 | 2h | 手动 | 开发人员 |
| L5 | 1h | 手动 | 开发人员 |
| **合计** | **~4~6h** | **L0+L1+L3 可脚本化(50min)** | — |

---

## 调度依赖说明（B5-2）

> ✅ B5-1.1 已完成（deep, 1m44s）

### 调度架构

```
FactorCollector
  触发: 每日 15:30 CST
  命令: cmd /c D:\futures_v6\macro_engine\scripts\run_factor_collector.bat daily
  compute_ic_heatmap.py: ❌ 未调用 → B5-1.2 将添加

DailyScoring
  触发: 工作日 14:30 CST
  命令: C:\Python311\python.exe D:\futures_v6\macro_engine\scripts\daily_scoring.py
  compute_ic_heatmap.py: ❌ 未调用
```

### 依赖链

```
FactorCollector (15:30)
    ↓ run_factor_collector.bat
因子采集脚本
    ↓ 成功
compute_ic_heatmap.py  ← B5-1.2 将追加
    ↓
ic_heatmap 表更新

DailyScoring (14:30)
    ↓
macro_scoring_engine.py
    ↓ 成功
CSV 信号输出
```

### B5-2 验收标准

- [x] schtasks 列出 FactorCollector 触发时间 ✅（每日 15:30）
- [x] schtasks 列出 DailyScoring 触发时间 ✅（工作日 14:30）
- [ ] FactorCollector 末尾调用 compute_ic_heatmap.py（待 B5-1.2）
- [ ] run_all 成功后 ic_heatmap 有增量更新记录
