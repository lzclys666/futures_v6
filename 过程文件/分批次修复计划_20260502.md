# 期货智能交易系统 V6.0 分批次修复计划

**制定日期**: 2026-05-02
**最后更新**: 2026-05-03
**GitHub 仓库**: https://github.com/lzclys666/futures_v6（已推送，含所有历史代码）
**风险标注**: 🔴 凭据已在 GitHub 公开（详见 D-3）
**依据**: 《代码审查报告》《解耦审查报告》《爬虫脚本审查报告》三份文档
**总工作量**: 约 120 人时(不含测试回归时间)
**建议节奏**: 2 人并行,每批次完成后做集成测试

---

## 批次总览

| 批次 | 主题 | 天数 | 核心目标 | 前置依赖 |
|------|------|------|---------|---------|
| **B1** | 安全止血 | 2d | 消除安全红线、修复核心bug | 无 |
| **B2** | 架构解耦 | 3d | 统一bridge、清理硬编码、统一风控规则 | B1 |
| **B3** | 爬虫规范 | **2.5~3d** | 修复爬虫脚本、建立跨品种调度、补全降级逻辑、脚本质量规范 | 无(可并行B1/B2) |
| **B4** | 数据一致性 | 2d | 统一字段映射、API契约、模型定义 | B2 |
| **B5** | 功能夯实 | 3d | 接入风控引擎、策略重构、补全测试 | B2+B4 |
| **B6** | 长期增强 | 持续 | 前端扩展、日志统一、运维完善 | B5 |

> **并行策略**: B1/B2/B3 互不依赖,可三线程并行。B4 依赖 B2,B5 依赖 B2+B4,B6 依赖 B5。

---

## B1 批次:安全止血(2天)

**目标**: 消除所有安全红线,修复会导致交易亏损的核心bug。此批次**不可跳过、不可延期**。

### B1-1 凭据迁移(0.5天)

| # | 任务 | 文件 | 工作量 | 验收标准 |
|---|------|------|--------|---------|
| B1-1.1 | 新建 `.env` 文件,迁移全部密码/令牌 | 新建 `.env` | 1h | 所有凭据从源码移除,`.env` 加入 `.gitignore` |
| B1-1.2 | run.py 移除 CTP 密码/账号默认值 | `run.py` | 0.5h | `os.environ.get("VNPY_CTP_PASSWORD")` 无 fallback |
| B1-1.3 | config/gateway_config.json 凭据外部化 | `config/gateway_config.json` | 0.5h | JSON 中无密码,改为 `${ENV:VNPY_CTP_PASSWORD}` 或程序启动时注入 |
| B1-1.4 | settings.yaml 凭据外部化 | `macro_engine/config/settings.yaml` | 0.5h | 同上 |
| B1-1.5 | 删除/清理含凭据的测试文件 | `_simple_ctp_test.py`, `test_ctp_connect.py` 等 | 0.5h | 确认这些测试文件是否仍在使用,若不用则删除,若用则改为从环境变量读取 |
| B1-1.6 | Tushare Token 外部化 | `macro_engine/scripts/scheduler.py` | 0.5h | `TUSHARE_TOKEN = os.getenv("TUSHARE_TOKEN")` |

**状态**: 🔴 **用户决定暂不处理（operator 指令），但 GitHub 已公开——凭据已泄露**
- `run.py:319` 仍有 `os.environ.get("VNPY_CTP_PASSWORD", "luzc19891222@")` fallback
- `run.py:70` 仍硬编码 `datafeed.password = "4545ed6ddce0c4071a9bc2695fd505afffc165eb"`（XTP token）
- `config/gateway_config.json:4` 仍硬编码 `"密码": "luzc19891222@"`
- `macro_engine/config/settings.yaml:26` 仍硬编码 `password: "luzc19891222@"`
- `_simple_ctp_test.py:18` 仍硬编码 `'密码': 'luzc19891222@'`
- `macro_engine/scripts/scheduler.py:7` 仍硬编码 `TUSHARE_TOKEN`
- **GitHub 已推送（https://github.com/lzclys666/futures_v6），上述 6 个文件全部公开在仓库中**
- 可重启条件：代码 push GitHub 之前必须完成凭据迁移

**建议操作顺序**（用户未选定，当前均未执行）：
1. 改 SimNow 密码（立刻，无成本）
2. 仓库改为 private（Settings → Change visibility → Private）
3. 创建 `.env` 文件迁出凭据
4. 下次 push 前：grep 确认源码零凭据

**风险**: 迁移后需要更新本地开发环境配置文档,否则团队成员无法启动。

### B1-2 PaperBridge 核心bug修复(0.5天)

| # | 任务 | 文件 | 工作量 | 验收标准 |
|---|------|------|--------|---------|
| B1-2.1 | 修复 cancel_order 返回值 | `services/vnpy_bridge.py` | 1h | 返回撤单结果字符串而非 bool,调用处兼容处理 |
| B1-2.2 | 复核 PaperBridge 盈亏计算 | `services/vnpy_bridge.py` | 2h | 写 3 个单元测试覆盖开/平/部分平仓场景,验证持仓均价计算正确 |

### B1-3 bridge 单例冲突(0.5天)

| # | 任务 | 文件 | 工作量 | 验收标准 |
|---|------|------|--------|---------|
| B1-3.1 | 统一 bridge 获取入口 | `macro_api_server.py`, `api/routes/*.py` | 3h | 所有 routes 统一通过 `get_vnpy_bridge()` 获取实例,禁止模块级 `from services.vnpy_bridge import bridge` |
| B1-3.2 | 启动时序确认 | `run.py` | 1h | 确保 `set_vnpy_bridge()` 在 `vnpy_bridge.start()` 之后调用,或改为懒加载 |

### B1-4 风控规则紧急对齐(0.5天)

| # | 任务 | 文件 | 工作量 | 验收标准 |
|---|------|------|--------|---------|
| B1-4.1 | 提取风控规则唯一源 | 新建 `core/risk/rules_config.yaml` | 3h | 将 R1-R12 的阈值、名称、层级、严重级别统一定义,四套代码全部读取此 YAML |
| B1-4.2 | 修改 routes/risk.py 读取统一配置 | `api/routes/risk.py` | 1h | `/api/risk/rules` 返回 YAML 中的规则定义 |
| B1-4.3 | 修改 PaperBridge 读取统一配置 | `services/vnpy_bridge.py` | 1h | `get_risk_status()` 读取同一 YAML |
| B1-4.4 | 修改 macro_api_server.py 读取统一配置 | `api/macro_api_server.py` | 0.5h | `get_risk_rules()` 读取同一 YAML |

**交付物**:
- [ ] `.env` 文件(含凭据清单说明)
- [ ] `core/risk/rules_config.yaml`
- [ ] B1-2.2 的单元测试 3 个用例全部通过
- [ ] 启动脚本可正常加载,无凭据报错

---

## B2 批次:架构解耦(3天)

**目标**: 清理 88+ 处硬编码,统一模块接口,消除耦合。

### B2-1 路径配置化(1天)

| # | 任务 | 文件 | 工作量 | 验收标准 |
|---|------|------|--------|---------|
| B2-1.1 | 建立项目根目录常量 | 新建 `config/paths.py` | 1h | `PROJECT_ROOT = Path(__file__).parent.parent`,所有模块导入此常量 |
| B2-1.2 | 迁移 macro_scoring_engine 路径 | `api/macro_scoring_engine.py` | 0.5h | `CSV_BASE_DIR = PROJECT_ROOT / "macro_engine" / "output"` |
| B2-1.3 | 迁移 routes/signal.py 路径 | `api/routes/signal.py` | 1h | 11 处硬编码改为 `PROJECT_ROOT` 拼接 |
| B2-1.4 | 迁移策略路径 | `strategies/macro_demo_strategy.py` 等 | 1h | `csv_path_str` 改为配置读取 |
| B2-1.5 | 迁移爬虫公共模块路径 | `common/check_au_data.py`, `_shared/exchange/*.py` | 1h | 10 处硬编码改为动态计算或配置 |
| B2-1.6 | 迁移数据库连接路径 | 所有 `sqlite3.connect('D:/futures_v6/...')` | 1h | 统一使用 `PROJECT_ROOT / "macro_engine" / "pit_data.db"` |

### B2-2 删除死代码副本(0.5天)

| # | 任务 | 文件 | 工作量 | 验收标准 | 状态 |
|---|------|------|--------|---------|------|
| B2-2.1 | 删除 risk/ 根目录副本 | `risk/risk_engine.py` | 0.5h | 删除文件,修改 `tests/test_risk_engine.py` 的 import 为 `core.risk.risk_engine` | **✅ 已完成**(2026-05-03 确认 `risk/` 根目录已无 `risk_engine.py`) |
| B2-2.2 | 确认无其他文件引用根目录 risk/ | `grep -r "from risk"` | 0.5h | 全局搜索确认无遗漏引用 | **✅ 已完成**(2026-05-03 `grep -rn "import risk_engine\|from risk import"` 全局无命中) |

### B2-3 routes/risk.py 复用 RiskEngine(1天)

| # | 任务 | 文件 | 工作量 | 验收标准 | 状态 |
|---|------|------|--------|---------|------|
| B2-3.1 | 重写 `/api/risk/simulate` 端点 | `api/routes/risk.py` | 4h | 调用 `RiskEngine.check_order()` 替代自研 300 行逻辑,保留 API 契约不变 | **✅ 已完成**(2026-05-03 确认 `simulate_risk()` 已委托 `RiskEngine.check_order()`,含 `_infer_exchange()` 和 `_simulate_fallback()`) |
| B2-3.2 | 添加 fallback:RiskEngine 不可用时降级 | `api/routes/risk.py` | 2h | 若 RiskEngine 未初始化,返回 503 或降级到简单检查 | **✅ 已完成**(内联于 B2-3.1,`engine is None` 时调用 `_simulate_fallback()`) |
| B2-3.3 | 回归测试 | `tests/` | 2h | `/api/risk/simulate` 的返回格式与修改前一致 | ⬜ 待执行 |

**遗留问题**(2026-05-03 检查确认,范围扩大):
- `api/routes/risk.py` 仍有 **5 处** `from services.vnpy_bridge import bridge`(第 194、232、259、291、523 行),未统一使用 `_BridgeProxy` 或 `get_vnpy_bridge()`。
- **新增发现**:`api/routes/trading.py:6`、`api/routes/position.py:39`、`api/routes/vnpy.py:9`、`api/macro_api_server.py:74` 也存在相同问题。
- **影响**:`api/routes/vnpy.py` 为模块级导入(非函数内),启动时即触发,耦合最严重;`trading.py` 涉及下单流程,直接影响交易执行。
- **修复方式**:所有 routes 统一改为通过 `get_vnpy_bridge()` 获取实例,或直接从 `RiskEngine._rules` 读取规则配置。

### B2-4 IP/端口配置化(0.5天)

| # | 任务 | 文件 | 工作量 | 验收标准 |
|---|------|------|--------|---------|
| B2-4.1 | CTP 服务器地址配置化 | `run.py`, `_simple_ctp_test.py` | 1h | 从环境变量或配置读取 SimNow/实盘地址 |
| B2-4.2 | API 基地址配置化 | `vite.config.ts`, 前端代码 | 1h | `127.0.0.1:8000` 改为从 `.env` 读取 `VITE_API_BASE_URL` |
| B2-4.3 | Redis 端口配置化 | `macro_engine/config/settings.yaml` | 0.5h | 6379 改为可配置,默认值保持 |

**交付物**:
- [ ] `config/paths.py` 文件
- [ ] 全局硬编码路径清理完毕(grep 确认 `D:/futures_v6` 仅出现在注释或 `.md` 文档中)
- [ ] `/api/risk/simulate` 回归测试通过
- [ ] `tests/test_risk_engine.py` 通过(import 已修正)

---

## B3 批次:爬虫规范(2天)

**目标**: 修复爬虫脚本的异常处理、降级逻辑、跨品种调度。可与 B1/B2 并行执行。

### B3-1 ZN_PB_SN 脚本重写(0.5天)

| # | 任务 | 文件 | 工作量 | 验收标准 |
|---|------|------|--------|---------|
| B3-1.1 | 重命名并重构脚本 | `ZN_PB_SN/ZN_PB_SN_LME升贴水.py`(新建) | 2h | 规范命名、定义 `FACTOR_CODE`/`SYMBOL`、加入 `save_to_db()` |
| B3-1.2 | 添加 L1/L2/L3/L4 降级逻辑 | 同上 | 2h | L1 失败后切 L2(备用数据源),L2 失败后 L4 兜底 |
| B3-1.3 | 移除 CSV 写入,改为统一入库 | 同上 | 1h | 删除 3 处硬编码 CSV 路径,数据通过 `save_to_db()` 入 pit_data.db |
| B3-1.4 | 删除旧文件 | `fetch_lme_zn_pb_sn.py` | 0.5h | 确认 ZN/PB/SN 的 run_all 调用新脚本 |

### B3-2 CU_NI 调用规范化(0.5天)

| # | 任务 | 文件 | 工作量 | 验收标准 |
|---|------|------|--------|---------|
| B3-2.1 | 建立跨品种因子注册表 | 新建 `_shared/cross_symbol_factors.yaml` | 1h | 声明 CU→[CU_NI/LME升贴水], NI→[CU_NI/LME升贴水], ZN/PB/SN→[ZN_PB_SN/LME升贴水] |
| B3-2.2 | 编写跨品种调度器 | 新建 `_shared/cross_symbol_runner.py` | 2h | 读取注册表,按品种调度跨品种因子脚本,返回统一数据结构 |
| B3-2.3 | 修改 CU_run_all.py 调用方式 | `CU/CU_run_all.py` | 1h | 删除硬编码 `../CU_NI/...`,改为调用 `cross_symbol_runner.run_for("CU")` |
| B3-2.4 | 修改 NI_run_all.py 调用方式 | `NI/NI_run_all.py` | 0.5h | 同上 |
| B3-2.5 | 修改 ZN/PB/SN run_all 调用方式 | `ZN/ZN_run_all.py` 等 | 1h | 同上 |

### B3-3 异常处理修复(0.5天)

| # | 任务 | 文件 | 工作量 | 验收标准 | 状态 |
|---|------|------|--------|---------|------|
| B3-3.1 | 修复 4 处 `except: pass` | AU_SPDR/BU_沥青/JM_计算焦煤月差/JM_计算焦煤期现基差 | 1h | 改为 `except Exception as e: print(f"[WARN] ...: {e}")` | **✅ 已完成**(2026-05-03 `grep -rn "except: pass"` 全局无命中) |
| B3-3.2 | 修复 13 文件裸 except | AO/EC/LC/LH/NI/P_run_all 等 | 2h | 全部改为 `except Exception as e`,保留 Ctrl+C 能力 | **✅ 已完成**(同上,全局裸 except 已清零) |
| B3-3.3 | 修复 DEVNULL 抑制 | AU/CU/I run_all.py | 1h | `stdout=subprocess.DEVNULL` 改为 `capture_output=True`,子进程 stderr 打印到主进程日志 | ⬜ 待确认(未在本次检查范围内) |

### B3-4 L4 兜底逻辑补全(0.5天)

| # | 任务 | 文件 | 工作量 | 验收标准 |
|---|------|------|--------|---------|
| B3-4.1 | 制定爬虫脚本模板 | 新建 `_shared/factor_template.py` | 2h | 模板包含 L1/L2/L3/L4 标准结构,所有新脚本从此模板复制 |
| B3-4.2 | 为高频因子补 L4 | AG/AU 等核心品种的关键因子 | 2h | 挑选 20 个最重要的因子脚本,在异常分支中加入 `save_l4_fallback()` |
| B3-4.3 | 验证 L4 调用率 | 全局 grep | 0.5h | `save_l4_fallback` 调用率从 4.5% 提升到至少 30% |

### B3-5 脚本质量补全(0.5天)【审查报告遗漏补充】

| # | 任务 | 文件 | 工作量 | 验收标准 | 来源 |
|---|------|------|--------|---------|------|
| B3-5.1 | 补全编码声明 `# -*- coding: utf-8 -*-` | **全部 322 个 .py 文件**(2026-05-03 确认 0 个有声明) | 2h | 所有含中文注释/中文文件名的脚本头添加编码声明;可用脚本批量注入 | 爬虫报告 M4 |
| B3-5.2 | 统一 `run_all` 主函数命名 | 全部 `*_run_all.py`(当前 `def main()` 与 `def run_all()` 混用) | 1h | 全部统一为 `def run_all():`,调用处同步修改 | 爬虫报告 P2-8 |
| B3-5.3 | 各品种目录添加 `__init__.py` | `macro_engine/crawlers/` 下 31 个品种目录 | 1h | 每个目录一个空 `__init__.py`,使目录可被 `import` | 爬虫报告 P2-9 |
| B3-5.4 | 清理 crawlers/ 根目录杂项 | `macro_engine/crawlers/` 根目录 17 个 .py 文件 | 1h | 区分一次性工具 vs 常驻脚本,工具移入 `tools/` 或删除 | 爬虫报告 P2-10 |
| B3-5.5 | 删除 NR 遗留 `.py.old` | `macro_engine/crawlers/NR/NR_抓取库存.py.old` | 0.25h | 确认无引用后删除 | 爬虫报告 P2-11 |

### B3-6 代码复用规范(0.5天)【审查报告遗漏补充】

| # | 任务 | 文件 | 工作量 | 验收标准 | 来源 |
|---|------|------|--------|---------|------|
| B3-6.1 | CU_NI 复用公共日期函数 | `CU_NI/CU_NI_抓取LME升贴水_EVENT.py` | 1h | 删除自建 `get_pit_date()`,改为调用 `common.db_utils.get_pit_dates()`,确保周一回退逻辑与公共模块一致(回退 3 天到上周五) | 爬虫报告 H3 |
| B3-6.2 | 推动脚本复用 `web_utils.fetch_url()` | 约 20+ 个裸用 `requests.get()` 的脚本 | 4h | 挑选高频/核心因子脚本优先改造;统一处理超时、重试、UA 头 | 爬虫报告 P2-12 |

**交付物**:
- [ ] `_shared/cross_symbol_factors.yaml`
- [ ] `_shared/cross_symbol_runner.py`
- [ ] `_shared/factor_template.py`
- [ ] ZN_PB_SN 新脚本可正常入库
- [ ] `except: pass` 全局清零

---

## B4 批次:数据一致性(2天)

**目标**: 统一前后端数据契约,消除字段映射混淆。依赖 B2(解耦完成)。

### B4-1 模型定义统一(0.5天)

| # | 任务 | 文件 | 工作量 | 验收标准 |
|---|------|------|--------|---------|
| B4-1.1 | 删除 macro_api_server.py 中的冗余 FactorDetail | `api/macro_api_server.py` | 1h | 删除该文件内重复定义的模型 |
| B4-1.2 | 统一使用 schemas.py 的模型 | `api/routes/signal.py` 等 | 1h | 所有路由使用 `schemas.FactorDetail` 序列化 |
| B4-1.3 | 修正 normalized_score vs factor_value | `api/schemas.py`, `macro_scoring_engine.py` | 1h | 确认字段语义:normalized_score = 归一化后的因子值,factor_value = 原始值,两者都保留但命名不混淆 |

### B4-2 CSV 字段映射对齐(0.5天)

| # | 任务 | 文件 | 工作量 | 验收标准 |
|---|------|------|--------|---------|
| B4-2.1 | 明确 factor_direction 与 contributionPolarity 语义 | `api/macro_scoring_engine.py`, `services/signal_bridge.py` | 2h | `factor_direction` = 因子固有方向属性(positive/negative/neutral,来自 YAML 配置);`contributionPolarity` = 该因子对当前信号的符号贡献(由 factor_value × weight 计算得出) |
| B4-2.2 | 修正混淆的映射代码 | `api/macro_scoring_engine.py` | 1h | 两处使用场景分开处理,不再互相替换 |
| B4-2.3 | 更新前端类型定义 | `frontend/types/signal.ts` | 1h | 前端类型与后端模型字段名一致 |

### B4-3 前端 API 契约对齐(0.5天)

| # | 任务 | 文件 | 工作量 | 验收标准 |
|---|------|------|--------|---------|
| B4-3.1 | 统一规则 ID 命名 | `frontend/types/risk.ts`, `api/routes/risk.py` | 2h | 规则 ID 完全一致,建立对照表文档 |
| B4-3.2 | 统一 API 响应格式 | `api/routes/risk.py` | 2h | 所有路由使用 `ApiResponse` 包装,返回 `{"code": 0, "message": "success", "data": ...}` |

**遗留问题**(2026-05-03 检查确认):
- `frontend/types/risk.ts` 第 81 行 `RiskSeverity` 仍为 `'PASS' | 'LOW' | 'MEDIUM' | 'HIGH'`,未同步改为 `'PASS' | 'WARN' | 'BLOCK'`(与后端 `routes/risk.py` 及 `risk/rules/risk_rules.yaml` 一致)。
- 虽然规则 ID 字符串已对齐(R3_PRICE_LIMIT 等),但 Severity 枚举值仍不一致,会导致前端 TypeScript 编译时类型不匹配,或运行时渲染错误的风控级别颜色/图标。
- **修复方式**:将 `RiskSeverity` 改为 `'PASS' | 'WARN' | 'BLOCK'`,同步修改所有引用该类型的组件(RuleSimulatorPage、RiskPanel 等)中的颜色映射逻辑。

### B4-4 mock 数据标识(0.5天)

| # | 任务 | 文件 | 工作量 | 验收标准 |
|---|------|------|--------|---------|
| B4-4.1 | 添加 is_mock 标记 | `api/macro_scoring_engine.py` | 1h | 当 CSV 不存在使用模拟数据时,返回字段中加入 `"is_mock": true` |
| B4-4.2 | 前端显示 mock 标识 | `frontend/` | 2h | 信号面板中 mock 数据用灰色/虚线标识,避免用户误判 |

**交付物**:
- [ ] `docs/api_contract.md`(API 契约文档,含字段对照表)
- [ ] 前端 types/risk.ts 与后端 routes/risk.py 规则 ID 100% 对齐
- [ ] `/api/macro/signal/all` 返回含 `is_mock` 字段

---

## B5 批次:功能夯实(3天)

**目标**: 让风控引擎真正工作,策略代码可维护,核心模块有测试保护。依赖 B2+B4。

### B5-1 风控引擎接入交易流程(1天)

| # | 任务 | 文件 | 工作量 | 验收标准 |
|---|------|------|--------|---------|
| B5-1.1 | 在 macro_demo_strategy 中集成 RiskEngine | `strategies/macro_demo_strategy.py` | 3h | `on_order()` / `send_order()` 前调用 `risk_engine.check_order()`,BLOCK 时拒绝下单 |
| B5-1.2 | 在 macro_risk_strategy 中复用同一套检查 | `strategies/macro_risk_strategy.py` | 2h | 不再自行实现风控检查,统一委托给 RiskEngine |
| B5-1.3 | run.py 加载风控策略 | `run.py` | 1h | 确保 `macro_risk_strategy.py` 被正确加载,而非默认加载无风控的 demo 策略 |
| B5-1.4 | 验证风控拦截生效 | 手工测试 | 2h | 在超出风控阈值时(如持仓超限),下单被拦截并返回明确原因 |

### B5-2 策略代码重构(0.5天)

| # | 任务 | 文件 | 工作量 | 验收标准 |
|---|------|------|--------|---------|
| B5-2.1 | 提取 MacroBaseStrategy 基类 | 新建 `strategies/macro_base_strategy.py` | 3h | 包含 `SYMBOL_CONFIG`、`extract_symbol()`、`get_symbol_config()`、`write_log_safe()`、`load_macro_signal()` 等公共方法 |
| B5-2.2 | 让 macro_demo_strategy 继承基类 | `strategies/macro_demo_strategy.py` | 1h | 删除重复代码,仅保留 demo 特有的逻辑 |
| B5-2.3 | 让 macro_risk_strategy 继承基类 | `strategies/macro_risk_strategy.py` | 1h | 同上 |
| B5-2.4 | 隔离 strategies 对 core/risk 的直接依赖 | `strategies/macro_risk_strategy.py` | 2h | 风控上下文通过 CtaTemplate 的 main_engine 间接获取,或从参数传入 |

### B5-3 核心模块测试补全(1.5天)

| # | 任务 | 文件 | 工作量 | 验收标准 |
|---|------|------|--------|---------|
| B5-3.1 | RiskEngine 规则链测试 | `tests/test_risk_engine.py` | 3h | 测试 `check_order()` 的规则链执行顺序、BLOCK 短路逻辑、多规则同时触发 |
| B5-3.2 | macro_scoring_engine 测试 | `tests/test_scoring_engine.py` | 3h | 测试 CSV 读取、mock 模式、Pipeline 4 节点输出 |
| B5-3.3 | PaperBridge 测试 | `tests/test_paper_bridge.py` | 3h | 测试开/平/部分平、持仓均价、盈亏计算 |
| B5-3.4 | API 路由测试 | `tests/test_api_routes.py` | 3h | 测试 `/api/macro/signal/all`、`/api/risk/status`、`/api/risk/simulate` 的返回格式和错误处理 |

**交付物**:
- [ ] `strategies/macro_base_strategy.py`
- [ ] 风控拦截测试用例通过(至少 5 个场景)
- [ ] 测试覆盖率报告(目标:risk_engine 80%+,scoring_engine 70%+)

---

## B6 批次:长期增强(持续迭代)

**目标**: 系统可观测、可运维、可扩展。依赖 B5。

### B6-1 日志体系统一(1周)

| # | 任务 | 文件 | 工作量 |
|---|------|------|--------|
| B6-1.1 | 建立统一 Logger 配置 | 新建 `config/logging.yaml` | 4h |
| B6-1.2 | 替换 run.py 中的 print | `run.py` | 2h |
| B6-1.3 | 统一 VNpyBridge 日志格式 | `services/vnpy_bridge.py` | 2h |
| B6-1.4 | 统一 API 层日志 | `api/macro_api_server.py` | 2h |
| B6-1.5 | 统一策略层日志 | `strategies/*.py` | 4h |

### B6-2 前端扩展(2周)

| # | 任务 | 工作量 |
|---|------|--------|
| B6-2.1 | 情报中心面板 | 24h |
| B6-2.2 | 策略管理面板 | 16h |
| B6-2.3 | 风控设置面板(`PUT /api/risk/rules/{rule_id}` 实际生效) | 8h |
| B6-2.4 | 统一前端 mock 数据标识 | 4h |

### B6-3 运维增强(1周)

| # | 任务 | 工作量 |
|---|------|--------|
| B6-3.1 | pit_data.db 自动备份 | 4h |
| B6-3.2 | 爬虫失败告警(邮件/企业微信) | 8h |
| B6-3.3 | 因子有效性监控(IC衰减告警) | 16h |
| B6-3.4 | run.py 与 API 服务进程分离 | 8h |

### B6-4 性能优化(1周)

| # | 任务 | 工作量 |
|---|------|--------|
| B6-4.1 | CSV → Parquet 双写渐进迁移 | 8h |
| B6-4.2 | 宏观打分引擎缓存(当日结果内存缓存) | 4h |
| B6-4.3 | TWAP 拆单 + 撤单追单 | 12h |

---

## 附录 A:任务依赖图

```
B1 ──┬──→ B2 ──┬──→ B4 ──┬──→ B5 ──┬──→ B6
     │         │         │         │
     └──→ B3 ──┘         │         │
                         │         │
                         └─────────┘
```

- B1、B3 可并行启动
- B2 依赖 B1 完成(凭据迁移后才能安全启动服务)
- B4 依赖 B2(路径配置化后才能统一模型)
- B5 依赖 B2+B4(解耦+数据一致后才能接入风控和重构策略)
- B6 依赖 B5

## 附录 B:每日站会检查清单

每个批次完成后,用以下清单验收:

- [ ] **安全**: grep 确认源码中无密码/令牌/密钥(`grep -ri "password\|token\|secret" --include="*.py"` 仅出现在环境变量读取代码中)
- [ ] **安全-补充**: `.env` 文件已存在且 `.gitignore` 已排除,源码中无 `luzc19891222@` 等凭据硬编码
- [ ] **bridge 统一**: `api/routes/risk.py` 中无 `from services.vnpy_bridge import bridge`(全部使用 `_BridgeProxy` 或 `get_vnpy_bridge()`)
- [ ] **前端 Severity**: `frontend/types/risk.ts` 中 `RiskSeverity = 'PASS' | 'WARN' | 'BLOCK'`
- [ ] **构建**: `python run.py --dry-run` 无报错启动
- [ ] **测试**: `pytest tests/` 全部通过(无 skip)
- [ ] **API**: 前端可正常加载信号面板、风控面板
- [ ] **爬虫**: `python CU/CU_run_all.py` 正常执行,无 `except: pass`
- [ ] **Git**: 所有变更已 commit,commit message 说明批次和任务编号

## 附录 C:风险与应急预案

| 风险 | 概率 | 影响 | 预案 |
|------|------|------|------|
| B1 凭据迁移后团队无法启动 | 中 | 高 | 提前写好 `.env.example`,发团队群,当面确认一人成功启动 |
| B2-3 routes/risk.py 重写后 API 格式变化 | 中 | 高 | 重写前用 Postman 导出当前 API 响应,重写后逐字段比对 |
| B3 爬虫脚本批量修改引入新bug | 高 | 中 | 每个品种修改后运行 run_all,观察 1 分钟无异常再下一个 |
| B5-1 风控接入后正常单被误拦截 | 中 | 高 | 先在 PaperBridge 模式运行 1 周,确认无误再切实盘 |
| B5-3 测试补全发现更多bug | 高 | 中 | 预期内,发现即修,不隐瞒,单独记录到 bug 列表 |

## 附录 D:遗留问题跟踪(2026-05-03 更新)

以下问题在解耦 6 步执行过程中发现,已确认尚未解决,需纳入后续批次修复:

| # | 问题 | 状态 | 所属批次 | 检查依据 |
|---|------|------|---------|---------|
| D-1 | `frontend/futures_trading/frontend/src/types/risk.ts:81` `RiskSeverity` 未同步为 `PASS \| WARN \| BLOCK` | ❌ 未解决 | B4-3 | `grep -n "RiskSeverity" frontend/futures_trading/frontend/src/types/risk.ts` → 仍为 `'PASS' \| 'LOW' \| 'MEDIUM' \| 'HIGH'` |
| D-2 | `api/routes/risk.py` 仍有 5 处 `from services.vnpy_bridge import bridge`;**新增**:`trading.py`、`position.py`、`vnpy.py`、`macro_api_server.py` 也存在 | ❌ 未解决 | B2-3 补充 / B1-3.1 | `grep -rn "from services.vnpy_bridge import bridge" api/` → **共 9 处**命中 |
| D-3 | `.env` 凭据迁移尚未启动，6 个文件仍含硬编码凭据；**GitHub 已推送，凭据公开泄露** | ❌ 未解决 | B1-1 | `test -f .env` → 不存在；grep 确认 6 处凭据硬编码（含 TUSHARE_TOKEN）；**GitHub 仓库 https://github.com/lzclys666/futures_v6 已公开，含全部凭据文件** |
| D-4 | 硬编码绝对路径 `D:/futures_v6` 扩散至回测/检查/生成脚本,共 **125 处** | ❌ 未解决 | B2-1 | `grep -rn "D:/futures_v6" --include="*.py"` → 全局 **125 处**命中: `signal.py`(11处)、`_shared/exchange/`(9处)、`phase3_v2_collect*.py`(8处)、`test_*.py`(20+处)、`scripts/*.py`(10+处)、其余 60+处分散 |

**影响评估**:
- D-1(前端 Severity):中等 - 会导致前端类型与后端不一致,渲染风控级别时可能出错
- D-2(bridge 直接导入):**中高** - `vnpy.py` 为模块级导入,启动即耦合;`trading.py` 涉及下单流程,直接影响交易执行
- D-3(凭据硬编码):**高** - 密码仍在源码中,push 到 GitHub 即泄露,必须在任何代码提交前完成
- D-4(硬编码路径):**中→中高** - 从预估 17 处扩大至 **125 处**,回测/测试/脚本全面沦陷,项目无法迁移目录

## 附录 E:审查报告覆盖情况审计(2026-05-03)

### E.1 《解耦审查报告》覆盖情况

| 报告章节 | 问题数 | 已覆盖批次 | 覆盖状态 |
|----------|--------|-----------|---------|
| 第一部分:硬编码清单(凭据/路径/IP/常量) | 88+ 处 | B1-1, B2-1, B2-4 | **✅ 全覆盖** |
| 第二部分:C1 风控规则四套定义 | 1 项 | B1-4 | **✅ 全覆盖** |
| 第二部分:C2 bridge 双注入路径 | 1 项 | B1-3, B2-3 | **✅ 全覆盖** |
| 第二部分:C3 routes/risk.py 自研风控 | 1 项 | B2-3 | **✅ 已修复** |
| 第二部分:C4 FactorDetail 模型不一致 | 1 项 | B4-1 | **✅ 全覆盖** |
| 第二部分:C5 CSV 字段映射混淆 | 1 项 | B4-2 | **✅ 全覆盖** |
| 第二部分:C6 前端 API 契约不一致 | 1 项 | B4-3 | **✅ 全覆盖** |
| 第二部分:C7 strategies 直接依赖 core | 1 项 | B5-2.4 | **✅ 全覆盖** |
| 第二部分:C8 core/risk 存在两个副本 | 1 项 | B2-2 | **✅ 已修复** |
| 第四部分:P0 立即执行(D-1~D-4) | 4 项 | B1-1, B1-3, B1-4, B2-2 | **✅ 全覆盖** |
| 第四部分:P1 本周完成(D-5~D-9) | 5 项 | B2-1, B2-3, B4-1, B4-2, B4-3 | **✅ 全覆盖** |
| 第四部分:P2 两周内(D-10~D-13) | 4 项 | B5-2.4, B2-4, B6 | **✅ 全覆盖** |
| **解耦报告合计** | **20+ 项** | - | **100% 覆盖** |

### E.2 《爬虫脚本审查报告》覆盖情况

| 报告章节 | 问题数 | 已覆盖批次 | 覆盖状态 | 备注 |
|----------|--------|-----------|---------|------|
| H1 CU_run_all.py 跨目录硬编码 | 1 项 | B3-2 | **✅ 已覆盖** | |
| H2 ZN_PB_SN 脚本质量不合格 | 1 项 | B3-1 | **✅ 已覆盖** | |
| H3 CU_NI 自建 get_pit_date() | 1 项 | **B3-6.1** | **⚠️ 本次补充** | 原遗漏 |
| M1 `except: pass` 静默异常 | 4 处 | B3-3.1 | **✅ 已修复** | |
| M2 裸 except 捕获 | 13 文件 | B3-3.2 | **✅ 已修复** | |
| M3 DEVNULL 抑制子进程输出 | 3 文件 | B3-3.3 | **✅ 已覆盖** | 待执行 |
| M4 编码声明缺失 | **322 文件** | **B3-5.1** | **⚠️ 本次补充** | 原遗漏;**2026-05-03 确认 0/322 有声明** |
| M5 公共模块硬编码路径 | 2 文件 | B2-1.5 | **✅ 已覆盖** | |
| L1 NR `.py.old` 遗留 | 1 文件 | **B3-5.5** | **⚠️ 本次补充** | 原遗漏;**2026-05-03 确认仍存在 1 个** |
| L2 crawlers/ 根目录杂项 | 17 文件 | **B3-5.4** | **⚠️ 本次补充** | 原遗漏;**2026-05-03 确认根目录仍有 17 个 .py** |
| L3 品种目录缺 `__init__.py` | 31 目录 | **B3-5.3** | **⚠️ 本次补充** | 原遗漏;**2026-05-03 确认 0/35 个目录有 `__init__.py`** |
| L4 run_all 命名不统一 | 全部 run_all | **B3-5.2** | **⚠️ 本次补充** | 原遗漏;**2026-05-03 确认 19 个 `*_run_all.py` 仍用 `def main()`** |
| L5 裸 `requests.get()` | ~20+ 脚本 | **B3-6.2** | **⚠️ 本次补充** | 原遗漏;**2026-05-03 确认仅 1 处引用 `web_utils`** |
| 四级降级模式 / L4 兜底 | 全局评估 | B3-4 | **✅ 已覆盖** | |
| P0 优化建议(3 项) | 3 项 | B3-1, B3-2, B3-3 | **✅ 已覆盖** | |
| P1 优化建议(4 项) | 4 项 | B2-1.5, B3-3.3, **B3-5.1** | **⚠️ 部分补充** | 编码声明为本次补充 |
| P2 优化建议(5 项) | 5 项 | **B3-5.2~5.5, B3-6.2** | **⚠️ 本次补充** | 全部 5 项原均遗漏 |
| P3 长期(模板+logging) | 2 项 | B3-4.1, B6-1 | **✅ 已覆盖** | |
| **爬虫报告合计** | **40+ 项** | - | **100% 覆盖(含本次补充 7 项)** | 原覆盖约 75%,补充后 100% |

### E.3 补充项汇总(本次新增 7 项)

| 编号 | 任务 | 批次 | 优先级 | 来源 |
|------|------|------|--------|------|
| 1 | 322 个脚本补编码声明 `# -*- coding: utf-8 -*-` | B3-5.1 | P1 | 爬虫报告 M4 |
| 2 | 统一 `run_all` 主函数命名 | B3-5.2 | P2 | 爬虫报告 P2-8 |
| 3 | 31 个品种目录添加 `__init__.py` | B3-5.3 | P2 | 爬虫报告 P2-9 |
| 4 | 清理 crawlers/ 根目录 17 个杂项 .py | B3-5.4 | P2 | 爬虫报告 P2-10 |
| 5 | 删除 NR `.py.old` 遗留文件 | B3-5.5 | P2 | 爬虫报告 P2-11 |
| 6 | CU_NI 复用公共 `get_pit_dates()` | B3-6.1 | P0 | 爬虫报告 H3 |
| 7 | 推动脚本复用 `web_utils.fetch_url()` | B3-6.2 | P2 | 爬虫报告 P2-12 |

> **工作量影响**:原 B3 批次评估 2 天,补充 B3-5 + B3-6 后建议调整为 **2.5~3 天**。

## 附录 F:2026-05-03 01:43 复查确认详情

本次复查针对"新发现/扩大的 2 项问题 + 爬虫报告遗漏补充 7 项"共 9 项问题,逐条用 grep 验证当前代码状态。

### F.1 架构层问题(D-2 / D-4)

| # | 问题 | 复查结果 | 数据 |
|---|------|---------|------|
| D-2 | bridge 直接导入 | ❌ **9 处全部仍在** | `api/routes/vnpy.py:9`(模块级), `trading.py:6`(模块级), `risk.py`(5处), `position.py:39`, `macro_api_server.py:74` |
| D-4 | 硬编码路径 `D:/futures_v6` | ❌ **从 17 处扩大至 125 处** | `signal.py`(11), `_shared/exchange/`(9), `phase3_v2_collect*.py`(8), `test_*.py`(20+), `scripts/*.py`(10+), 其余分散 60+ |

### F.2 爬虫脚本问题(B3-5 / B3-6)

| # | 问题 | 复查结果 | 数据 |
|---|------|---------|------|
| B3-5.1 | 编码声明 | ❌ **0/322** | `grep -rn "# -*- coding: utf-8 -*-" macro_engine/crawlers/` → **0 命中** |
| B3-5.2 | run_all 命名统一 | ❌ **19 个仍用 `def main()`** | `grep -rn "def main()" macro_engine/crawlers/ --include="*_run_all.py"` → **19 命中** |
| B3-5.3 | `__init__.py` | ❌ **0/35** | `find macro_engine/crawlers/ -maxdepth 1 -name "__init__.py"` → **0 命中** |
| B3-5.4 | 根目录杂项 | ❌ **仍有 17 个** | `ls macro_engine/crawlers/*.py` → **17 个文件** |
| B3-5.5 | NR `.py.old` | ❌ **仍有 1 个** | `find macro_engine/crawlers/NR -name "*.py.old"` → **1 命中** |
| B3-6.1 | CU_NI `get_pit_date()` | ❌ **自建版本仍在** | `grep -n "def get_pit_date" macro_engine/crawlers/CU_NI/*.py` → **第 32 行命中** |
| B3-6.2 | `web_utils` 复用 | ❌ **仅 1 处引用** | `grep -rn "web_utils" macro_engine/crawlers/` → **1 命中**(应为 20+) |

### F.3 结论

以上 **9 项问题全部未启动修复**。

- **最危险**:D-2 的 `trading.py` + `vnpy.py` 模块级导入——启动时序调整直接炸交易/API
- **最广泛**:D-4 的 125 处硬编码路径——回测/测试/脚本全面沦陷,远超预估
- **最易批量修复**:B3-5.1(编码声明)、B3-5.3(`__init__.py`)、B3-5.5(删旧文件)——可用脚本一次性完成

**建议执行顺序**:D-3(凭据) → D-2(bridge) → D-4(路径) → B3-5.1~5.5(脚本质量) → B3-6.1~6.2(代码复用)

---

_此计划随实际执行进度动态调整,每周五回顾一次,更新剩余工作量和风险状态。_
