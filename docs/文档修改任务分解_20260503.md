# 📝 文档修改任务分解 — 2026-05-03

> 整理人：PM  
> 日期：2026-05-03  
> 背景：今日完成 C1-C8架构修复 + B1-B5批次 + P0/P1接口一致性修复，所有技术文档需同步更新。

---

## 一、已确认无需修改的文档 ✅

以下文档今日修复**不涉及业务逻辑变更**，无需更新：

| 文档 | 原因 |
|------|------|
| `risk/` 下两本风控手册 | 业务规则未变，架构修复不影响描述 |
| `系统架构/VNpyBridge_API接口规范.md` | C1-C8为内部实现修复，不改变对外接口行为 |
| `macro_engine/因子采集脚本审查计划书_v2.0/v2.1.md` | 爬虫脚本已合规，审查结论不变 |

---

## 二、需要修改的文档清单

### 🟡 需验证 + 小改（各自检查是否有残留旧内容）

#### A. shared/ 层 — YIYI 负责

| 文档 | 检查要点 | 备注 |
|------|---------|------|
| `shared/EVENTS_CONTRACT.md` | 接口待 PaperBridge 实现，现状描述是否准确 | 预计无需大改 |
| `shared/CSV_DATA_CONTRACT.md` | ic_heatmap 表 schema 是否反映 B3 新增的 target_symbol 列 | B3 已加列，需确认 |

#### B. macro_engine/ 层 — YIYI 负责

| 文档 | 检查要点 | 备注 |
|------|---------|------|
| `macro_engine/FACTOR_GOVERNANCE.md` | ic_heatmap 品种目标≥20，AL/J 暂缺原因是否说明 | B4 ic_heatmap 方案已确定 |
| `macro_engine/SPEC_EXECUTION_PLAN.md` | B1-B5 各批次完成状态是否标记为 ✅ | 当前可能仍显示 ⏳ |

---

### 🟠 需确认 + 可能修改（deep/Lucy/YIYI/mimo 各自查）

#### C. modules/ 层 — 各负责人

| 文档 | 负责人 | 检查要点 |
|------|--------|---------|
| `modules/backend/API_Reports_合集.md` | deep | routes/risk.py 拆分后端点列表是否准确 |
| `modules/backend/开发指南.md` | deep | API 调用方式是否反映 compositeScore [-1,1] 范围 |
| `modules/frontend/开发指南.md` | Lucy | RiskStatusData 字段变更（triggeredCount/circuitBreaker）是否说明 |
| `modules/frontend/深度审查报告.md` | Lucy | phantom 字段修复是否记录 |
| `modules/factors/IC热力图修复报告.md` | YIYI | compute_ic_heatmap.py 修复内容是否记录 |
| `modules/engineering/ARCHITECTURE.md` | mimo | C1-C8 解耦内容是否记录 |

---

### 🔴 需更新（PM 负责）

#### D. 核心治理文档 — PM 直接修改

| 文档 | 修改内容 | 优先级 |
|------|---------|--------|
| `docs/修复执行计划书_20260503.md` | 补充 P0-7/P1-7/P2 项完成状态（今日 21:38 全部验收通过） | 高 |
| `docs/接口一致性审计报告_20260503.md` | 补充两轮审查 P0 全部修复确认 | 高 |
| `macro_engine/SPEC_EXECUTION_PLAN.md` | B1-B5 批次状态更新为 ✅ | 中 |

---

## 三、各成员任务确认清单

请各成员收到后**回复确认**，有疑问请发给我（PM）汇总。

**回复格式：**
```
[成员名] 确认收到
- 我负责的文档：xxx.md, yyy.md
- 有疑问：xxx（发给PM汇总）
- 或：无疑问，本地修改后提交
```

### 负责人分配

| 成员 | 负责文档 | 核心检查项 |
|------|---------|-----------|
| **deep** | `modules/backend/API_Reports_合集.md` + `开发指南.md` | routes/risk.py 拆分 + compositeScore 范围 |
| **Lucy** | `modules/frontend/开发指南.md` + `深度审查报告.md` | phantom 字段修复 + RiskStatusData |
| **YIYI** | `macro_engine/FACTOR_GOVERNANCE.md` + `SPEC_EXECUTION_PLAN.md` + `shared/CSV_DATA_CONTRACT.md` | ic_heatmap + B1-B5 状态 |
| **mimo** | `modules/engineering/ARCHITECTURE.md` | C1-C8 解耦修复记录 |
| **PM** | `docs/修复执行计划书_20260503.md` + `docs/接口一致性审计报告_20260503.md` | 直接修改 |

---

## 四、今日修复内容摘要（供对照）

### 架构层（C1-C8）
- C1：bridge 双注入 → `services/vnpy_bridge.py` 懒加载单例
- C2：routes 绕过 engine → `api/routes/risk.py` 独立 router
- C3：FactorDetail 两套 → `api/macro_scoring_engine.py` 统一
- C4：规则 ID 错位 → `risk_rules.yaml` + 前后端 RuleId 对齐
- C5：phantom 字段 → `types/macro.ts` 删除 equity/drawdown/macroCircuit
- C6：ConfigProvider darkAlgorithm → 前端 ConfigProvider 修复
- C7：YAML 语义验证工具 → `scripts/verify_yaml_semantics.py`
- C8：API 路由不一致 → `API_CONTRACT.md` + `INTERFACE_GOVERNANCE.md`

### 接口层（P0/P1/P2，今日 21:38 全部验收通过）
- P0-1：`validateRiskStatusData` 校验值 `'正常'|'告警'|'触发'` → `'PASS'|'WARN'|'BLOCK'`
- P0-2：`routes/risk.py` 三层响应格式统一（与 Pydantic 模型对齐）
- P0-3：`trading.ts` 删除重复 client，复用 `risk.ts`
- P0-4：`compositeScore` [0,1] → [-1,1] 转换（4处 CSV 读取路径）
- P1：`INTERFACE_GOVERNANCE.md` R4 名称修正 + R12 新增 + /api/risk/* 路径
- P2：`test_api_e2e_part2.py` 断言 `levels` → `rules`；R10 同步修复

### 验证脚本结果（23:08，9/9 PASS）
```
[PASS] B1: vnpy_bridge.py range(1,13) ✅
[PASS] B2: routes/risk.py _RULE_LAYERS 字典 ✅
[PASS] F1: macro_scoring_engine.py compositeScore 转换 ×4 ✅
[PASS] D3: API_CONTRACT.md /api/risk/rules 路径 ✅
[PASS] L1: RiskPage.tsx LAYER_CONFIG 数值键 ✅
[PASS] L2: PositionBoard.tsx RISK_STATUS_MAP 无中文 ✅
[PASS] L3: RiskControlPanel.tsx dataSource→rules ✅
[PASS] D1: TYPE_CONTRACT.md 无 phantom RiskStatusResponse ✅
[PASS] D2: macro.ts RiskStatusData 正确 ✅
```

---

## 五、疑问汇总通道

如有疑问，**直接发给我（PM）**，我在这里汇总后统一处理。

不明确的地方：
1. 某文档是否需要更新？
2. 修复内容与某文档描述冲突？
3. 发现新的不一致？

请发给我，我来判定。
