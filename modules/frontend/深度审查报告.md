# 前端代码深度审查报告 v2（完整验证版）

**审查范围**：`D:\futures_v6\macro_engine\frontend\futures_trading\src`
**审查日期**：2026-04-27
**审查人**：Deep Review v2
**状态**：🔴 发现 22 个问题（5 P0 / 9 P1 / 8 P2）

---

## 一、用户清单逐项验证结果

### 用户清单中完全不存在的文件/问题（6 项）

| # | 用户提到的问题 | 实际情况 |
|---|--------------|---------|
| P0-1 | `vite.config.ts` 端口 22395 | 实际是 `8000`，已正确 |
| P0-2 | `@` alias 无效 | `path.resolve(__dirname, './src')` 已正确 |
| P0-3 | `RiskControlPanel.tsx` | **文件不存在** |
| P1-5 | `types/macro.ts` 的 `PositionItem` | **不存在此类型** |
| P1-6 | `api/macro.ts` 的 `_adapt` | **不存在此函数** |
| P1-8 | `SignalDailyReport.tsx` | **文件不存在** |

### 用户清单中需修正描述的问题（4 项）

| # | 用户提到的问题 | 实际情况 |
|---|--------------|---------|
| P1-7 | `handlePrecheck` 竞态 | `handlePrecheck` 不存在，实际是 `handleSubmit`，未发现竞态 |
| P2-1 | `/risk/config` 不高亮 | 需确认：精确匹配逻辑与菜单配置共同决定行为 |
| P2-5 | 加载状态永远 false | 部分正确：`loading` 有定义但从未在 JSX 中使用（死代码）|
| — | `placeOrder` 返回值不匹配 | `vt_orderid`（snake_case）vs `vtOrderId`（camelCase），后端格式未确认 |

---

## 二、所有问题清单（22 项）

### 🔴 P0 — 致命（编译/运行时必崩，5 项）

| # | 问题 | 文件 | 影响 |
|---|------|------|------|
| P0-1 | `usePolling` Hook 不存在，两页引用必崩 | `pages/macro/index.tsx`、`pages/positions/index.tsx` | 运行时 `Cannot find module` |
| P0-2 | `RiskConfig` 两套实现各自维护，规则 ID 不一致 | `pages/risk/config.tsx` vs `pages/risk/config/index.tsx` | 配置逻辑长期不同步 |
| P0-3 | `index.css` 全局样式 `text-align: center` + `width: 1126px` | `index.css` | 破坏 Ant Design 所有组件布局 |
| P0-4 | MainLayout Sider/Header `background: '#fff'` 硬编码 | `layouts/MainLayout.tsx` | 暗色模式切了也是白页面 |
| P0-5 | ConfigProvider 未接入 `theme.darkAlgorithm` | `App.tsx` | `darkMode` 状态完全无效 |

### 🟠 P1 — 功能错误或严重不一致（9 项）

| # | 问题 | 文件 | 影响 |
|---|------|------|------|
| P1-1 | `status` 与 `severity` 两套系统混用，颜色判断逻辑矛盾 | `types/risk.ts`、`RiskDashboard.tsx` | 颜色语义不统一 |
| P1-2 | `riskStore.ts` 中 R6 用 `R6_CORRELATION`（应为 `R6_LIQUIDITY`），layer 也错 | `store/riskStore.ts` | 规则 ID 与规范不符 |
| P1-3 | `config.tsx` 的 `RULE_META` 规则 ID 与 `riskStore` fallback 完全对不上 | `pages/risk/config.tsx`、`store/riskStore.ts` | 配置页面读不到正确的阈值 |
| P1-4 | `fetchAllSignals` 用 `r.data!` 非空断言，`r.data` 实为 `T | null` | `api/macro.ts` | 隐式 null 风险 |
| P1-5 | `FactorDashboard` 的 `loading` 状态从未在 JSX 中使用 | `pages/factor-dashboard/index.tsx` | 死代码，Mock 加载无感知 |
| P1-6 | 菜单同时有 `/risk` 和 `/risk/config` 两个独立项，`selectedKeys` 单选行为不确定 | `layouts/MainLayout.tsx` | `/risk/config` 可能不高亮 |
| P1-7 | `factorGet` 返回裸数据 `T`，`apiGet` 返回 `ApiResponse<T>`，混用易出错 | `api/client.ts`、`api/macro.ts` | 调用方需记住哪些函数要 `.data` 提取 |
| P1-8 | `placeOrder` 返回 `vt_orderid`（snake_case），mock 用 `vtOrderId`（camelCase）| `api/vnpy.ts`、`store/vnpyStore.ts` | 与后端对接字段名不确定 |
| P1-9 | `runPrecheck` 是空函数，所有订单直接通过，无风控保护 | `store/tradingStore.ts` | Phase 1 阶段风控形同虚设 |

### 🟡 P2 — 可改进（8 项）

| # | 问题 | 文件 | 备注 |
|---|------|------|------|
| P2-1 | PDF 导出仅为 `window.print()` | `pages/report/index.tsx` | 非真实 PDF |
| P2-2 | Phase 2 专用组件全部缺失（共 9 个）| 组件层 | 待 Phase 2 开发 |
| P2-3 | Zustand Store 未开 `devtools` | `store/*.ts` | 无法 Redux DevTools 调试 |
| P2-4 | API 端点路径未与 deep 确认 | `api/*.ts` mock 数据 | 待对齐 |
| P2-5 | `RiskConfig` Layer 分组逻辑在两处实现中不一致 | `config.tsx` vs `config/index.tsx` | 三分法 vs 市场/账户/执行 |
| P2-6 | CSS 令牌（`--text`/`--bg`）与 Ant Design Token 系统脱节 | `index.css`、`App.tsx` | 两套令牌并存不关联 |
| P2-7 | 组件缺少 JSDoc 和 `Props` interface | 组件层 | 代码可维护性 |
| P2-8 | 缺少全局小白引导（术语提示/引导流程/帮助页）| 全局 | 用户体验 |

---

## 三、分步骤修复计划

### 🔧 第零步：摸清家底（P0-2 关联，准备工作）

**目的**：解决 P0-2 和 P1-3、P1-2 的根源——规则 ID 系统不一致。

| 步骤 | 操作 | 工时 | 责任 |
|------|------|------|------|
| 0.1 | 确认规范：找出路线图定义的 11 条规则的标准 ID 和 Layer 分组（市场风险 L1 / 账户风险 L2 / 交易执行 L3） | — | YIYII |
| 0.2 | 以规范为准，在 `riskStore.ts` fallback 中统一规则 ID（`R1`~`R11`），统一 `layer` 值 | 30 分钟 | deep/mimo |
| 0.3 | 删掉 `pages/risk/config/index.tsx`（简单版），`config.tsx` 完整版作为唯一实现 | 5 分钟 | deep/mimo |
| 0.4 | 对齐 `config.tsx` 的 `RULE_META` 与 riskStore fallback 的规则 ID（必须完全一致） | 30 分钟 | deep/mimo |

---

### 🔧 第一步：止血 — 让项目能跑起来（P0-1 / P0-3 / P0-4 / P0-5）

**目的**：消除编译/运行时崩溃，修复暗色模式基础建设。

| 步骤 | 操作 | 工时 | 责任 |
|------|------|------|------|
| 1.1 | **实现 `usePolling` hook**：`hooks/usePolling.ts`，参照 `useWebSocket` 模式实现（接受 URL + interval，返回 `{ data, loading, error }`）；在 `hooks/index.ts` 导出 | 30 分钟 | deep/mimo |
| 1.2 | **修复 `index.css`**：删除 `text-align: center`（在 `#root` 上）；`width` 改为 `100%`；`body font-size` 从 `18px` 改为 `14px`（Ant Design 默认） | 15 分钟 | deep/mimo |
| 1.3 | **修复 MainLayout Sider/Header**：删除 `style={{ background: '#fff' }}`，让 Ant Design 默认样式生效；用 CSS 变量或 Ant Design token 控制背景色 | 15 分钟 | deep/mimo |
| 1.4 | **接入 ConfigProvider 暗色算法**：在 `App.tsx` 引入 `theme.darkAlgorithm`；`darkMode` 状态通过 Context 下传，`ConfigProvider` 的 `theme.algorithm` 根据 `darkMode` 动态切换 | 1 小时 | deep/mimo |

**第一步完成后**：项目可启动，路由可达，暗色模式可切换。

---

### 🔧 第二步：修类型 — 消灭暗雷（P1-1 / P1-4 / P1-7 / P1-8）

**目的**：统一类型系统，消除运行时隐患。

| 步骤 | 操作 | 工时 | 责任 |
|------|------|------|------|
| 2.1 | **统一 Severity/Status**：在 `types/risk.ts` 中明确定义——`RiskRuleConfig.status`（`'ENABLED' \| 'DISABLED'`），`RiskRuleStatus.severity`（`'PASS' \| 'HIGH' \| 'MEDIUM' \| 'LOW'`）；重写 `RiskDashboard.tsx` 颜色判断逻辑 | 2 小时 | kiki |
| 2.2 | **修复 `fetchAllSignals` null 安全**：将 `r.data!` 改为 `r.data ?? null`，用可选链处理 | 15 分钟 | kiki |
| 2.3 | **统一 API 返回值类型**：将 `factorGet/factorPost` 也包装为返回 `ApiResponse<T>`，调用方统一用 `.data` 提取；或在 `api/macro.ts` 中每个函数单独处理解包 | 1 小时 | deep/mimo |
| 2.4 | **确认 `placeOrder` 字段名**：与 deep 确认后端返回 `vt_orderid`（snake）还是 `vtOrderId`（camel），统一 `api/vnpy.ts` 类型定义和 mock 数据 | 30 分钟 | deep + YIYII |

**第二步完成后**：类型系统一致，无隐式 null 风险，API 风格统一。

---

### 🔧 第三步：补功能断点（P1-5 / P1-6 / P1-9）

**目的**：Phase 1 完整功能交付。

| 步骤 | 操作 | 工时 | 责任 |
|------|------|------|------|
| 3.1 | **FactorDashboard loading 接入 JSX**：将 `loading` 状态用于 `<Spin>` 或 `<Skeleton>` 包装；或删除 `loading` 状态和 useEffect（若不需要） | 15 分钟 | kiki |
| 3.2 | **修复菜单高亮**：将 `/risk/config` 改为 `/risk` 的子菜单（Ant Design Menu `children` 字段），访问 `/risk/config` 时父级 `/risk` 高亮 | 30 分钟 | kiki |
| 3.3 | **`runPrecheck` 接入风控**：`tradingStore.runPrecheck` 改为调用 `riskStore.checkOrder`（Phase 1 等效实现：`riskStore.rules` 遍历 + 阈值比较）；`placeOrder` 前强制调用 `checkOrder` | 1 小时 | deep/mimo |

**第三步完成后**：Phase 1 功能完整，无死代码，风控生效。

---

### 🔧 第四步：Phase 2 前准备（P2-5 / P2-6）

**目的**：为 Phase 2 打好基础，减少后续重构。

| 步骤 | 操作 | 工时 | 责任 |
|------|------|------|------|
| 4.1 | **统一 RiskConfig Layer 分组**：以 `config.tsx` 的 `LAYER_INFO`（市场风险/账户风险/交易执行）为基准，`riskStore.ts` fallback 的 `layer` 值对齐 | 30 分钟 | deep/mimo |
| 4.2 | **CSS 令牌接入 Ant Design**：将 `index.css` 的 CSS 变量（`--text`、`--bg` 等）作为 Ant Design `ConfigProvider.theme.token` 的值来源；暗色模式下通过 CSS 变量媒体查询同步切换 | 2 小时 | kiki |

---

### 🔧 第五步：提升工程质量（不阻塞开发）

| 步骤 | 操作 | 工时 | 责任 |
|------|------|------|------|
| 5.1 | **Store 开启 devtools**：所有 zustand store 添加 `devtools: import.meta.env.DEV` 条件中间件 | 30 分钟 | kiki |
| 5.2 | **PDF 导出真实实现**：引入 `@react-pdf/renderer` 或 `jspdf`，替代 `window.print()` | 2 小时 | deep/mimo |
| 5.3 | **补充组件 JSDoc**：为核心组件（`RiskDashboard`、`ICHeatmap`、`SignalBrief`）添加 `Props` interface 和 docstring | 1 小时 | kiki |
| 5.4 | **API 路径与 deep 确认**：对照 mock 数据端点与 deep 的 CTP 接口文档，产出 `API_CONTRACT.md` | 待 deep | — |

---

### 🔧 第六步：Phase 2 组件开发（P2-2）

| 组件 | 说明 | 优先级 |
|------|------|--------|
| `DispositionModal` | 处置效应引导弹窗 | P0 Phase 2 |
| `VolatilityGauge` | 波动率仪表盘 | P1 |
| `LiquidityIndicator` | 流动性指标卡 | P1 |
| `CircuitBreaker` | 宏观熔断指示器 | P0 Phase 2 |
| `DailyLossBar` | 当日亏损进度条 | P0 Phase 2 |
| `LossCounter` | 连续亏损计数器 | P1 |
| `DispositionAlert` | 处置效应告警 Banner | P0 Phase 2 |
| `PositionLimitBar` | 仓位限制进度条 | P1 |
| `MarginBar` | 保证金占用 Bar | P1 |

---

## 四、工时汇总

| 阶段 | 步骤 | 涉及问题 | 合计工时 |
|------|------|---------|---------|
| 第零步 | 0.1~0.4 | P0-2、P1-2、P1-3 | ~1.5 小时 |
| 第一步 | 1.1~1.4 | P0-1、P0-3、P0-4、P0-5 | ~2 小时 |
| 第二步 | 2.1~2.4 | P1-1、P1-4、P1-7、P1-8 | ~3.5 小时 |
| 第三步 | 3.1~3.3 | P1-5、P1-6、P1-9 | ~2 小时 |
| 第四步 | 4.1~4.2 | P2-5、P2-6 | ~2.5 小时 |
| 第五步 | 5.1~5.4 | P2-1、P2-3、P2-4、P2-7 | ~3.5 小时 |
| **总计** | | | **~15 小时** |

---

## 五、总结

**最关键的 5 个 P0**（按修复顺序）：
1. `usePolling` 不存在 → 项目无法启动，**第一个修**
2. `index.css` 污染布局 → 所有组件显示异常，**第一个修**
3. ConfigProvider 未接入暗色算法 → 主题切换是空操作，**第一步同步修**
4. MainLayout 硬编码白色 → 暗色模式切了也是白，**第一步同步修**
5. RiskConfig 两套实现不同步 → 后续所有风控配置都会写到错误版本，**第零步先统一**

**修复完成后**：骨架完整 → 功能完整 → 质量提升 → Phase 2 组件开发。

---

*审查完成（v2）。建议严格按步骤执行，第零步和第一步可并行，第零步完成前不要动 P0-2 关联的规则 ID。*
